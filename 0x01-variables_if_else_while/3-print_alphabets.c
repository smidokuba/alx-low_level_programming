#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 */

int main(void)
{
	char n;
	char w;

	n = 'a';
	w = 'A';
	while
		(n <= 'z') {
			putchar(n);
			n++;
		}
	while
		(w <= 'Z') {
			putchar(w);
			w++;
		}
		putchar('\n');
		return (0);
}
